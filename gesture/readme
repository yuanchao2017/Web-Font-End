这个手势密码是针对移动端的，虽然界面有些粗糙，但是基本功能还是实现了。第一次用canvas实现界面，里面遇到了很多问题，总结如下：
1）宽高的设置：起初我是在css文件里面设置宽和高，但是出现了拉伸现象。解决方式：在canvas中直接设置width,height
2) 内联元素：canvas是内联元素，布局时需要注意
3）touchmove时，显示部分最好是重新画出相关的内容，这样不会出现问题
4）检查密码时，不要先判断长度，因为有时候会出现undefined情况，故还是根据内容值进行比较
